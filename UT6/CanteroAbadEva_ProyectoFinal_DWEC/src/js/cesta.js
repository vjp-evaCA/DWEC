// cesta.js - Página del carrito
import { cargarHeader, actualizarContadorCarrito } from './header.js';
import '../css/styles.css';

console.log('Página del carrito cargada');

document.addEventListener('DOMContentLoaded', () => {
    console.log('Cargando productos del carrito...');
    // Aquí cargarás productos desde IndexedDB
});